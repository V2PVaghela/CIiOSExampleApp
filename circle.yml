deployment:
  beta_distribution:
    branch: master
    commands:
      - gym
      - ipa distribute:hockeyapp
          --token            "3063c351a48a4d7d90dbffb0092abfc5"
          --notes            "CircleCI build 1.0"

machine:
  xcode:
    version: 7.3.1

test:
  override:
	- set -o pipefail && xcodebuild CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY= PROVISIONING_PROFILE= -workspace 'CIiOSExampleApp.xcworkspace' -scheme 'CIiOSExampleApp' -sdk iphonesimulator -destination 'id=44FFC543-D9A3-4E6F-BFA4-6F62180F3425' clean build test | tee $CIRCLE_ARTIFACTS/xcode_raw.log | xcpretty --color --report junit --output $CIRCLE_TEST_REPORTS/xcode/results.xml